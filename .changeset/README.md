# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).
Run `npm run changeset` to record a change to one or more packages; commit the
generated markdown file with your PR. On merge to `main`, the release workflow
turns accumulated changesets into version bumps + a publish.
